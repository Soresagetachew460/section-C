String Capitalization Script

This script uses Python's capitalize() method to format a string.

Usage

string = "country side"
new_string = string.capitalize()
print(new_string)  # Output: "Country side"
